# 只包含SAR测试图像的数据集配置
path: ../OptiSAR-Net/datasets/CORS-SAR_sar
train: images   # 无训练集，但 YAML 要求此项存在，可留空或指向 images
val: images     # 验证集也用 images（测试时实际使用 test 字段）
test: images

names:
  0: opt_plane
  1: sar_plane